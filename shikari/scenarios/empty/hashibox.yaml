# A scenario to launch a cluster without any configurations

plain: true

networks:
  - lima: shared

vmType: qemu

env:
  SHIKARI_SCENARIO_NAME: "empty"

images:
  - location: ~/.shikari/wizbox-fedora/wizbox.qcow2